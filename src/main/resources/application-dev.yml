spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/arm_dev_db
    username: postgres
    password: admin123

server:
  error:
    path: /error


aws:
  accessKeyId:
  secretKey:
  region:
  dynamodb:
    endpoint: